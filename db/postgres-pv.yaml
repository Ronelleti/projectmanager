apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: manual   # 🔥 IMPORTANT
  hostPath:
    path: /mnt/data/postgres   # local path inside minikube