apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "projectmanager.fullname" . }}-test-connection"
  labels:
    {{- include "projectmanager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "projectmanager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
